---
const socialLinks = [
  {
    name: "Email",
    icon: "📧",
    href: "mailto:hola@CristianDev.com",
    label: "hola@CristianDev.com",
    color: "#ea4335"
  },
  {
    name: "LinkedIn",
    icon: "💼",
    href: "https://linkedin.com/in/CristianDev",
    label: "/in/CristianDev",
    color: "#0077b5"
  },
  {
    name: "Twitter",
    icon: "🐦",
    href: "https://twitter.com/CristianDev",
    label: "@CristianDev",
    color: "#1da1f2"
  },
  {
    name: "GitHub",
    icon: "🐱",
    href: "https://github.com/CristianDev",
    label: "/CristianDev",
    color: "#333"
  },
  {
    name: "Twitch",
    icon: "🟣",
    href: "https://twitch.tv/CristianDev",
    label: "/CristianDev",
    color: "#9146ff"
  },
  {
    name: "YouTube",
    icon: "📺",
    href: "https://youtube.com/CristianDev",
    label: "/CristianDev",
    color: "#ff0000"
  }
];
---

<section id="contacto" class="contact">
  <div class="container">
    <div class="contact-content">
      <div class="contact-info">
        <div class="section-header">
          <h2 class="section-title">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"></path>
              <polyline points="22,6 12,13 2,6"></polyline>
            </svg>
            Contacto
          </h2>
          <p class="section-description">
            ¿Tienes un proyecto en mente? ¿Quieres colaborar? ¡Hablemos!
          </p>
        </div>

        <div class="contact-text">
          <p>
            Estoy siempre abierto a nuevas oportunidades y colaboraciones interesantes. 
            Si tienes un proyecto emocionante o simplemente quieres charlar sobre tecnología, 
            no dudes en contactarme.
          </p>
          <p>
            <span class="highlight">Tiempo de respuesta típico: 24-48 horas</span>
          </p>
        </div>

        <div class="contact-methods">
          <div class="method-item primary-method">
            <div class="method-icon">📧</div>
            <div class="method-info">
              <h3 class="method-title">Email</h3>
              <p class="method-description">Para proyectos y colaboraciones</p>
              <a href="mailto:hola@CristianDev.com" class="method-link highlight">hola@CristianDev.com</a>
            </div>
          </div>

          <div class="method-item">
            <div class="method-icon">💼</div>
            <div class="method-info">
              <h3 class="method-title">LinkedIn</h3>
              <p class="method-description">Conectemos profesionalmente</p>
              <a href="https://linkedin.com/in/CristianDev" class="method-link">linkedin.com/in/CristianDev</a>
            </div>
          </div>
        </div>
      </div>

      <div class="contact-form-container">
        <form class="contact-form" id="contact-form">
          <div class="form-group">
            <label for="name" class="form-label">Nombre *</label>
            <input 
              type="text" 
              id="name" 
              name="name" 
              class="form-input" 
              required 
              placeholder="Tu nombre completo"
            />
          </div>

          <div class="form-group">
            <label for="email" class="form-label">Email *</label>
            <input 
              type="email" 
              id="email" 
              name="email" 
              class="form-input" 
              required 
              placeholder="tu@email.com"
            />
          </div>

          <div class="form-group">
            <label for="subject" class="form-label">Asunto *</label>
            <input 
              type="text" 
              id="subject" 
              name="subject" 
              class="form-input" 
              required 
              placeholder="¿De qué quieres hablar?"
            />
          </div>

          <div class="form-group">
            <label for="message" class="form-label">Mensaje *</label>
            <textarea 
              id="message" 
              name="message" 
              class="form-textarea" 
              required 
              rows="5"
              placeholder="Cuéntame sobre tu proyecto o idea..."
            ></textarea>
          </div>

          <button type="submit" class="btn btn-primary form-submit">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <line x1="22" y1="2" x2="11" y2="13"></line>
              <polygon points="22,2 15,22 11,13 2,9 22,2"></polygon>
            </svg>
            Enviar mensaje
          </button>
        </form>
      </div>
    </div>

    <div class="social-links">
      <h3 class="social-title">También puedes encontrarme en:</h3>
      <div class="social-grid">
        {socialLinks.map((social, index) => (
          <a 
            href={social.href} 
            class="social-link" 
            target="_blank" 
            rel="noopener noreferrer"
            data-aos="fade-up" 
            data-aos-delay={index * 50}
          >
            <span class="social-icon">{social.icon}</span>
            <div class="social-info">
              <span class="social-name">{social.name}</span>
              <span class="social-handle">{social.label}</span>
            </div>
          </a>
        ))}
      </div>
    </div>
  </div>
</section>

<style>
  .contact {
    padding: 6rem 0;
    background: linear-gradient(180deg, #1a1b26 0%, #16161e 100%);
    position: relative;
  }

  .contact-content {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    margin-bottom: 4rem;
  }

  .section-header {
    margin-bottom: 2rem;
  }

  .section-title {
    display: inline-flex;
    align-items: center;
    gap: 0.75rem;
    font-size: 2.5rem;
    font-weight: 700;
    color: #c9d1d9;
    margin-bottom: 1rem;
  }

  .section-title svg {
    color: #7aa2f7;
  }

  .section-description {
    font-size: 1.125rem;
    color: #a9b1d6;
    line-height: 1.6;
  }

  .contact-text {
    margin-bottom: 2rem;
  }

  .contact-text p {
    color: #a9b1d6;
    line-height: 1.7;
    margin-bottom: 1rem;
  }

  .contact-methods {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }

  .method-item {
    display: flex;
    gap: 1rem;
    padding: 1.5rem;
    background: rgba(30, 31, 44, 0.5);
    border: 1px solid rgba(65, 72, 104, 0.3);
    border-radius: 1rem;
    backdrop-filter: blur(10px);
    transition: all 0.3s ease;
  }

  .method-item:hover {
    transform: translateY(-2px);
    border-color: rgba(122, 162, 247, 0.5);
  }

  .primary-method {
    border-color: rgba(224, 175, 104, 0.5);
    background: rgba(224, 175, 104, 0.1);
  }

  .method-icon {
    font-size: 1.5rem;
    width: 50px;
    height: 50px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(122, 162, 247, 0.1);
    border-radius: 0.75rem;
    flex-shrink: 0;
  }

  .method-title {
    font-size: 1.125rem;
    font-weight: 600;
    color: #c9d1d9;
    margin-bottom: 0.25rem;
  }

  .method-description {
    font-size: 0.875rem;
    color: #7c7c7c;
    margin-bottom: 0.5rem;
  }

  .method-link {
    color: #7aa2f7;
    text-decoration: none;
    font-weight: 500;
    transition: color 0.3s ease;
  }

  .method-link:hover {
    color: #9bb8f5;
  }

  .contact-form-container {
    background: rgba(30, 31, 44, 0.5);
    border: 1px solid rgba(65, 72, 104, 0.3);
    border-radius: 1rem;
    padding: 2rem;
    backdrop-filter: blur(10px);
  }

  .contact-form {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }

  .form-group {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .form-label {
    font-weight: 500;
    color: #c9d1d9;
    font-size: 0.875rem;
  }

  .form-input,
  .form-textarea {
    padding: 0.75rem 1rem;
    background: rgba(26, 27, 38, 0.8);
    border: 1px solid rgba(65, 72, 104, 0.5);
    border-radius: 0.5rem;
    color: #c9d1d9;
    font-size: 1rem;
    transition: all 0.3s ease;
    resize: vertical;
  }

  .form-input:focus,
  .form-textarea:focus {
    outline: none;
    border-color: #7aa2f7;
    box-shadow: 0 0 0 3px rgba(122, 162, 247, 0.1);
  }

  .form-input::placeholder,
  .form-textarea::placeholder {
    color: #565f89;
  }

  .form-submit {
    align-self: flex-start;
    margin-top: 0.5rem;
  }

  .social-links {
    text-align: center;
  }

  .social-title {
    font-size: 1.25rem;
    font-weight: 600;
    color: #c9d1d9;
    margin-bottom: 2rem;
  }

  .social-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 1rem;
    max-width: 800px;
    margin: 0 auto;
  }

  .social-link {
    display: flex;
    align-items: center;
    gap: 1rem;
    padding: 1rem;
    background: rgba(30, 31, 44, 0.5);
    border: 1px solid rgba(65, 72, 104, 0.3);
    border-radius: 0.75rem;
    color: inherit;
    text-decoration: none;
    backdrop-filter: blur(10px);
    transition: all 0.3s ease;
  }

  .social-link:hover {
    transform: translateY(-3px);
    border-color: rgba(122, 162, 247, 0.5);
    box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2);
  }

  .social-icon {
    font-size: 1.5rem;
    width: 40px;
    height: 40px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(122, 162, 247, 0.1);
    border-radius: 0.5rem;
    flex-shrink: 0;
  }

  .social-info {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    text-align: left;
  }

  .social-name {
    font-weight: 500;
    color: #c9d1d9;
    font-size: 0.875rem;
  }

  .social-handle {
    font-size: 0.75rem;
    color: #7c7c7c;
  }

  @media (max-width: 968px) {
    .contact-content {
      grid-template-columns: 1fr;
      gap: 3rem;
    }
  }

  @media (max-width: 768px) {
    .contact {
      padding: 4rem 0;
    }

    .section-title {
      font-size: 2rem;
    }

    .contact-form-container {
      padding: 1.5rem;
    }

    .social-grid {
      grid-template-columns: 1fr;
      max-width: 400px;
    }

    .method-item {
      padding: 1rem;
    }
  }

  @media (max-width: 480px) {
    .social-grid {
      grid-template-columns: 1fr;
    }

    .form-submit {
      align-self: stretch;
    }
  }
</style>

<script>
  const form = document.getElementById('contact-form');
  
  if (form) {
    form.addEventListener('submit', async (e) => {
      e.preventDefault();
      
      const submitBtn = form.querySelector('.form-submit');
      const originalText = submitBtn.innerHTML;
      
      submitBtn.innerHTML = `
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" class="animate-spin">
          <path d="M21 12a9 9 0 11-6.219-8.56"/>
        </svg>
        Enviando...
      `;
      submitBtn.disabled = true;
      
      setTimeout(() => {
        submitBtn.innerHTML = `
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <polyline points="20,6 9,17 4,12"></polyline>
          </svg>
          ¡Enviado!
        `;
        
        setTimeout(() => {
          submitBtn.innerHTML = originalText;
          submitBtn.disabled = false;
          form.reset();
        }, 3000);
      }, 2000);
    });
  }
</script>

<style>
  .animate-spin {
    animation: spin 1s linear infinite;
  }
  
  @keyframes spin {
    from {
      transform: rotate(0deg);
    }
    to {
      transform: rotate(360deg);
    }
  }
</style>
